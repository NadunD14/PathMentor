"""
Main entry point for PathMentor backend.
"""

import uvicorn

from app import app
from core.config import settings

if __name__ == "__main__":
    uvicorn.run(
        "app.main:app",
        host=settings.host,
        port=settings.port,
        reload=settings.debug,
        log_level="info"
    )